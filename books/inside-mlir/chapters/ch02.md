# 第 2 章 IR 的形状：Operation / Region / Block / Value 与 LLVM IR 对照

## 这一章想让你带走什么

<div class="goal-box">
<p>上一章说 MLIR「没有固定的指令集，只有骨架」。这一章就把骨架拆开：<strong>Operation、Region、Block、Value</strong> 四个概念，加上把它们连起来的 <strong>OpOperand</strong>。你会看到 <code>Operation</code> 在内存里是怎么一次性分配出来的、为什么 MLIR 不需要 <code>phi</code> 指令、为什么「一个函数」和「一个模块」在 MLIR 里都只是普通的 op。</p>
<p>拿走三样东西：一张能对着 LLVM IR 逐项翻译的对照表；一个浏览器里能跑的迷你 IR，你会亲手实现 <code>replaceAllUsesWith</code>——所有 pattern 重写背后的那个原子动作（第 14 章会用真家伙再做一遍）；以及一个此后天天要用的判断力：看到一段 MLIR，能立刻说出它有几层 region、哪些值跨了 region 边界、谁是 terminator。</p>
</div>

## 先摆一张对照表

你已经有 LLVM IR 的心智模型了，最快的路是逐项翻译。先记结论，后面逐条展开：

| LLVM IR | MLIR | 差别在哪 |
| --- | --- | --- |
| `Module` | `builtin.module` **op** | 模块不是特殊容器，就是一个普通 op |
| `Function` | `func.func` **op** | 函数也是 op；它的函数体是它的 region |
| `BasicBlock` | `Block` | 概念一致，但 block 可以有**参数** |
| `Instruction` | `Operation` | 指令集不固定；op 可以**嵌套 region** |
| `Value` | `Value` | 一致：SSA 值。来源只有两种（op 结果 / 块参数） |
| `Use` / `User` | `OpOperand` | 一致：一条 def-use 边是个对象 |
| `phi` 指令 | **块参数 + terminator 传参** | MLIR 没有 phi |
| 固定的类型系统 | 方言可注册类型 | `tensor`、`memref`、`!pix.kernel` 平等 |
| metadata（可丢） | **属性**（固有属性不可丢） | 语义有保证，见本章最后一节 |
| intrinsic | 就是普通 op | 不需要"特殊指令"这个概念 |

一句话总结这张表：**MLIR 把 LLVM IR 里那些"特殊的东西"（模块、函数、phi、intrinsic）全部化归成了同一个东西——op。**

## Operation：唯一的主角

MLIR 里几乎所有东西都是 `Operation`。一个 op 身上挂着这些部分：

- **名字**（`OperationName`）：`"arith.addf"` 这样的字符串，前缀就是方言名。注意名字是**运行时数据**，不是 C++ 枚举——这正是 op 可扩展的前提。
- **操作数**（operands）：一串 `Value`，每个操作数对应一条 def-use 边。
- **结果**（results）：一串 `Value`。可以是 0 个（`func.return`）、1 个、或多个。
- **属性 / 属性存储**（attributes、properties）：编译期常量数据，比如常量的值、函数名、`fastmath` 标志。
- **region**：0 到 N 个。这是 MLIR 与 LLVM IR 结构上最大的不同。
- **后继块**（successors）：只有分支类 op 才有。
- **位置**（`Location`）：**不可选**。每个 op 都带位置信息，报错才能指回源码。
- 以及它在哪个 `Block` 里。

这些东西在内存里不是一堆独立的 `std::vector`，而是**紧跟在 `Operation` 对象后面一次性分配出来的**。源码里这行类声明就是证据（`mlir/include/mlir/IR/Operation.h`）：

```cpp-norun
// mlir/include/mlir/IR/Operation.h
class alignas(8) Operation final
    : public llvm::ilist_node_with_parent<Operation, Block>,
      private llvm::TrailingObjects<Operation, detail::OperandStorage,
                                    detail::OpProperties, BlockOperand, Region,
                                    OpOperand> {
public:
  static Operation *create(Location location, OperationName name,
                           TypeRange resultTypes, ValueRange operands, ...);
```

读法：`TrailingObjects<Operation, A, B, C…>` 是 LLVM 里一个常用手法——**把若干个变长数组接在对象自己后面**，一次 `malloc` 搞定。所以一个 op 的操作数存储、属性存储、后继块、region 列表、以及结果对应的 `OpOperand`，全都在同一块内存里。`ilist_node_with_parent<Operation, Block>` 则说明 op 是**侵入式链表的节点**，它天生知道自己的父 block，在链表里插入删除都是 O(1)。

<div class="concept">
<svg viewBox="0 0 700 250" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
  <text x="14" y="20" font-size="13" font-weight="bold" fill="#24292f">一个 Operation 在内存里：一次分配，变长部分接在后面</text>
  <rect x="20" y="36" width="140" height="80" rx="6" fill="#eef4fb" stroke="#2b6cb0" stroke-width="2"/>
  <text x="34" y="56" font-size="12" font-weight="bold" fill="#2b6cb0">Operation</text>
  <text x="34" y="74" font-size="10" fill="#24292f">name（字符串化）</text>
  <text x="34" y="89" font-size="10" fill="#24292f">location（必有）</text>
  <text x="34" y="104" font-size="10" fill="#24292f">parent block 指针</text>
  <g font-size="10">
    <rect x="172" y="36" width="104" height="36" rx="5" fill="#f0f8f2" stroke="#3f9b5c"/>
    <text x="182" y="52" fill="#2f7a45" font-weight="bold">OperandStorage</text>
    <text x="182" y="66" fill="#6b7280">操作数（变长）</text>
    <rect x="172" y="80" width="104" height="36" rx="5" fill="#f0f8f2" stroke="#3f9b5c"/>
    <text x="182" y="96" fill="#2f7a45" font-weight="bold">OpProperties</text>
    <text x="182" y="110" fill="#6b7280">固有属性</text>
    <rect x="288" y="36" width="104" height="36" rx="5" fill="#f0f8f2" stroke="#3f9b5c"/>
    <text x="298" y="52" fill="#2f7a45" font-weight="bold">BlockOperand[]</text>
    <text x="298" y="66" fill="#6b7280">后继块（分支才有）</text>
    <rect x="288" y="80" width="104" height="36" rx="5" fill="#f0f8f2" stroke="#3f9b5c"/>
    <text x="298" y="96" fill="#2f7a45" font-weight="bold">Region[]</text>
    <text x="298" y="110" fill="#6b7280">嵌套区域</text>
    <rect x="404" y="58" width="104" height="36" rx="5" fill="#f0f8f2" stroke="#3f9b5c"/>
    <text x="414" y="74" fill="#2f7a45" font-weight="bold">OpOperand[]</text>
    <text x="414" y="88" fill="#6b7280">结果的 use 边</text>
  </g>
  <line x1="160" y1="76" x2="170" y2="76" stroke="#6b7280"/>
  <line x1="276" y1="76" x2="286" y2="76" stroke="#6b7280"/>
  <line x1="392" y1="76" x2="402" y2="76" stroke="#6b7280"/>
  <text x="524" y="70" font-size="11" fill="#e06a3b">一次 malloc</text>
  <text x="524" y="86" font-size="11" fill="#e06a3b">零指针跳转</text>
  <line x1="20" y1="140" x2="680" y2="140" stroke="#d0d7de" stroke-dasharray="4 3"/>
  <text x="14" y="164" font-size="12" font-weight="bold" fill="#6b7280">对比 LLVM IR 的 Instruction</text>
  <text x="14" y="184" font-size="11" fill="#24292f">指令种类固定（一张 .def 表），操作数个数基本固定，<tspan font-weight="bold">不能嵌套</tspan>——所以不需要变长 region 列表</text>
  <text x="14" y="204" font-size="11" fill="#24292f">MLIR 反过来：op 种类无限、操作数/结果/region 个数全不定，于是必须把"变长"做进内存布局里</text>
  <text x="14" y="230" font-size="11" fill="#e06a3b">代价：op 名是字符串化的、查询要过一层间接——这是"可扩展"换来的开销（第 13 章谈编译时间时会再提）</text>
</svg>
<div class="caption">图 2-1　Operation 的内存布局。理解这张图能解释很多现象：为什么 op 没有"构造函数"只有 `create`（要先算出变长部分的大小）、为什么改变操作数个数的 API 很少、以及为什么 MLIR 能在一个 IR 里容纳成千上万种 op</div>
</div>

<div class="keypoint">
<strong>三个要尽早接受的"都是 op"</strong>
<p><strong>模块是 op</strong>（<code>builtin.module</code>）：所以 pass 可以跑在模块上，也可以跑在任何 op 上，没有"顶层特殊处理"。<strong>函数是 op</strong>（<code>func.func</code>）：所以一个方言可以定义自己的"函数"（比如 <code>gpu.func</code>），不需要改框架。<strong>常量是 op</strong>（<code>arith.constant</code>）：MLIR 里没有"立即数操作数"这种东西，常量必须先由一个 op 产生出一个值——这点第 11 章讲折叠时会变得很关键。</p>
</div>

## Value 与 def-use：边也是对象

`Value` 就是 SSA 值。它的来源**只有两种**（`mlir/include/mlir/IR/Value.h`）：

- **`OpResult`**：某个 op 的第 n 个结果。
- **`BlockArgument`**：某个 block 的第 n 个参数。

没有第三种。函数参数不是特例——它们就是函数体 region 首块的块参数。

把值和 op 连起来的是 **`OpOperand`**：一条 def-use 边。关键点在于**边是一个对象，不是一根裸指针**。每个 `OpOperand` 知道自己属于哪个 op、是第几个操作数、指向哪个值；而每个值维护一条**侵入式链表**，串起所有指向它的 `OpOperand`（`mlir/include/mlir/IR/UseDefLists.h` 里的 `IRObjectWithUseList`，成员就叫 `firstUse`）。

这个设计带来一个极其常用的能力：`Value::replaceAllUsesWith(newValue)`——把「所有用我的地方」一次性改成用别人，代价与使用次数成正比，不需要扫整个函数。**MLIR 里几乎所有变换最后都落到这一句上。**

下面这段代码把 `Value` / `OpOperand` / `Operation` 三者照搬成 60 行 Python，然后做一次 `replaceAllUsesWith`，把第 1 章那个「乘 1.0」折掉。点运行，看 use list 怎么变：

```python
# 一个迷你 IR：把 MLIR 的 Value / OpOperand / Operation 三者的关系照搬出来，
# 然后做一次 replaceAllUsesWith——这是所有 pattern 重写背后的那个原子动作。

class Value:
    """SSA 值。它记着'谁在用我'（use list），而不只是'我被谁定义'。"""
    def __init__(self, name, defining_op=None):
        self.name = name
        self.defining_op = defining_op   # None 表示它是块参数（比如函数入口参数）
        self.uses = []                   # 元素是 OpOperand；MLIR 里这是一条侵入式链表

    def replace_all_uses_with(self, new_value):
        # MLIR 里这一句就是 Value::replaceAllUsesWith(newValue)
        moved = list(self.uses)
        for use in moved:
            use.set(new_value)           # 每条边改指向，顺手维护两端的 use list
        return len(moved)

    def __repr__(self):
        return self.name


class OpOperand:
    """一条 def-use 边。它是个对象，不是裸指针——它知道自己属于哪个 op 的第几个操作数。"""
    def __init__(self, owner, index, value):
        self.owner, self.index, self.value = owner, index, None
        self.set(value)

    def set(self, value):
        if self.value is not None:
            self.value.uses.remove(self)
        self.value = value
        value.uses.append(self)


class Operation:
    def __init__(self, name, operands, num_results=1):
        self.name = name
        self.operands = [OpOperand(self, i, v) for i, v in enumerate(operands)]
        short = name.split(".")[-1]
        self.results = [Value(f"%{short}{i}", self) for i in range(num_results)]

    def ins(self):
        return [o.value for o in self.operands]

    def __repr__(self):
        outs = ", ".join(r.name for r in self.results)
        lhs = outs + " = " if outs else ""
        return f"{lhs}{self.name} {', '.join(v.name for v in self.ins())}"


def dump(title, values, ops):
    print(f"--- {title} ---")
    for op in ops:
        print("  " + repr(op))
    print("  use list（谁在用这个值）：")
    for v in values:
        users = [f"{u.owner.name}#{u.index}" for u in v.uses]
        print(f"    {v.name:>8} -> {users if users else '（没人用）'}")
    print()


# 构造：%sum = addf %a, %b ; %out = mulf %sum, %one ; return %out
a, b, one = Value("%a"), Value("%b"), Value("%one")
addf = Operation("arith.addf", [a, b])
sum_v = addf.results[0]
mulf = Operation("arith.mulf", [sum_v, one])
out_v = mulf.results[0]
ret = Operation("func.return", [out_v], num_results=0)
ops = [addf, mulf, ret]
watch = [a, b, one, sum_v, out_v]
dump("构造完成", watch, ops)

# 做"乘 1.0 等于没乘"这个折叠：把 mulf 的结果，全部换成它的第一个操作数
n = out_v.replace_all_uses_with(sum_v)
print(f"replaceAllUsesWith：改写了 {n} 条 def-use 边")
print()
dump("折叠之后（mulf 还在，但已经没人用它的结果）", watch, ops)


def erase(op, ops):
    if any(r.uses for r in op.results):
        print(f"  {op.name} 的结果还有人用，不能删")
        return
    for use in list(op.operands):
        use.value.uses.remove(use)
    ops.remove(op)
    print(f"  删掉 {op.name}")


print("清理死代码：")
erase(mulf, ops)
print()
dump("最终 IR", watch, ops)
print("注意 %one 的 use list 也空了——定义它的那个常量 op 于是也成了死代码，")
print("会被同一趟清理顺手带走。这就是为什么 MLIR 里'折叠'和'删死代码'总是成对出现。")
```

运行结果：

```
--- 构造完成 ---
  %addf0 = arith.addf %a, %b
  %mulf0 = arith.mulf %addf0, %one
  func.return %mulf0
  use list（谁在用这个值）：
          %a -> ['arith.addf#0']
          %b -> ['arith.addf#1']
        %one -> ['arith.mulf#1']
      %addf0 -> ['arith.mulf#0']
      %mulf0 -> ['func.return#0']

replaceAllUsesWith：改写了 1 条 def-use 边

--- 折叠之后（mulf 还在，但已经没人用它的结果） ---
  %addf0 = arith.addf %a, %b
  %mulf0 = arith.mulf %addf0, %one
  func.return %addf0
  use list（谁在用这个值）：
          %a -> ['arith.addf#0']
          %b -> ['arith.addf#1']
        %one -> ['arith.mulf#1']
      %addf0 -> ['arith.mulf#0', 'func.return#0']
      %mulf0 -> （没人用）

清理死代码：
  删掉 arith.mulf

--- 最终 IR ---
  %addf0 = arith.addf %a, %b
  func.return %addf0
  use list（谁在用这个值）：
          %a -> ['arith.addf#0']
          %b -> ['arith.addf#1']
        %one -> （没人用）
      %addf0 -> ['func.return#0']
      %mulf0 -> （没人用）

注意 %one 的 use list 也空了——定义它的那个常量 op 于是也成了死代码，
会被同一趟清理顺手带走。这就是为什么 MLIR 里'折叠'和'删死代码'总是成对出现。
```

**这段输出就是第 1 章那条 `--canonicalize` 命令内部发生的事。** 真正的 `mlir-opt` 只是把「折叠 + RAUW + 删死 op」放进了一个带 worklist 的驱动器里反复跑（第 14 章）。

<div class="note">
<strong>两个细节值得注意</strong>
<p>一，折叠之后 <code>arith.mulf</code> <strong>还在 IR 里</strong>，只是没人用它的结果了——"改写"与"删除"是两个独立动作。二，判断一个 op 能不能删，不只看"有没有用户"，还要看它<strong>有没有副作用</strong>：一个没人用结果的 <code>memref.store</code> 绝不能删。这个信息由第 5 章的 <code>MemoryEffects</code> 接口提供，而 <code>Pure</code> 这个 trait 就是"无副作用 + 可推测执行"的简写。</p>
</div>

## Region 与 Block：嵌套是一等公民

现在到了 MLIR 与 LLVM IR 结构上真正分岔的地方。

- **`Block`** = 一串 op + **一组块参数** + 最后一个 op 是 **terminator**。
- **`Region`** = 一串 `Block`。
- 一个 op 可以有 **0 到 N 个 region**。

于是 IR 是一棵**树**：op 里有 region，region 里有 block，block 里又有 op……一路嵌套下去。LLVM IR 只有「函数 → 基本块 → 指令」这固定三层，且指令内部不能再套东西。

看一个真实的三层嵌套。这段代码求和，用 `scf.for` 带 `iter_args`（相当于循环携带的累加器）：

```mlir
func.func @sum(%buf: memref<8xf32>) -> f32 {
  %c0 = arith.constant 0 : index
  %c1 = arith.constant 1 : index
  %c8 = arith.constant 8 : index
  %init = arith.constant 0.0 : f32
  %total = scf.for %i = %c0 to %c8 step %c1 iter_args(%acc = %init) -> (f32) {
    %v = memref.load %buf[%i] : memref<8xf32>
    %next = arith.addf %acc, %v : f32
    scf.yield %next : f32
  }
  return %total : f32
}
```

把它用通用格式打印出来，嵌套结构就全露出来了：

```bash
mlir-opt sum.mlir --mlir-print-op-generic
```

运行结果：

```
"builtin.module"() ({
  "func.func"() <{function_type = (memref<8xf32>) -> f32, sym_name = "sum"}> ({
  ^bb0(%arg0: memref<8xf32>):
    %0 = "arith.constant"() <{value = 0 : index}> : () -> index
    %1 = "arith.constant"() <{value = 1 : index}> : () -> index
    %2 = "arith.constant"() <{value = 8 : index}> : () -> index
    %3 = "arith.constant"() <{value = 0.000000e+00 : f32}> : () -> f32
    %4 = "scf.for"(%0, %2, %1, %3) ({
    ^bb0(%arg1: index, %arg2: f32):
      %5 = "memref.load"(%arg0, %arg1) : (memref<8xf32>, index) -> f32
      %6 = "arith.addf"(%arg2, %5) <{fastmath = #arith.fastmath<none>}> : (f32, f32) -> f32
      "scf.yield"(%6) : (f32) -> ()
    }) : (index, index, index, f32) -> f32
    "func.return"(%4) : (f32) -> ()
  }) : () -> ()
}) : () -> ()
```

逐条对着看，有五个信息量很大的点：

1. **三层 region 清晰可见**：`builtin.module` 的 region 里装着 `func.func`，`func.func` 的 region 里装着循环，`scf.for` 的 region 里装着循环体。
2. **归纳变量 `%i` 和累加器 `%acc` 都是块参数**：`^bb0(%arg1: index, %arg2: f32)`。循环体不是"知道自己是循环体"的特殊结构，它只是一个带参数的 block。
3. **`scf.yield` 是 terminator**，它把这一轮的累加值交回给 `scf.for`；`scf.for` 的结果 `%4` 就是最后一轮 yield 的值。
4. **`%arg0`（那个 memref）被循环体直接引用了**——它定义在外层 region，但在内层可见。这是 MLIR 允许的：**内层 region 能引用外层已定义的值**，不必像函数调用那样把所有东西当参数传进去。
5. `scf.for` 的操作数是 `(%0, %2, %1, %3)`，即 lower/upper/step/init——自定义格式里那套漂亮的 `%i = %c0 to %c8 step %c1` 语法只是**打印出来的糖**（第 3 章、第 8 章讲它从哪来）。

<div class="concept">
<svg viewBox="0 0 700 290" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
  <text x="14" y="20" font-size="13" font-weight="bold" fill="#24292f">IR 是一棵树：op → region → block → op</text>
  <rect x="20" y="32" width="660" height="240" rx="8" fill="#eef4fb" stroke="#2b6cb0" stroke-width="2"/>
  <text x="32" y="50" font-size="11" font-weight="bold" fill="#2b6cb0">op: builtin.module</text>
  <rect x="36" y="58" width="628" height="204" rx="6" fill="#fff" stroke="#2b6cb0" stroke-dasharray="4 3"/>
  <text x="46" y="74" font-size="10" fill="#6b7280">region #0</text>
  <rect x="52" y="82" width="596" height="172" rx="6" fill="#eef4fb" stroke="#2b6cb0"/>
  <text x="62" y="98" font-size="11" font-weight="bold" fill="#2b6cb0">op: func.func @sum</text>
  <rect x="66" y="106" width="566" height="140" rx="6" fill="#fff" stroke="#2b6cb0" stroke-dasharray="4 3"/>
  <text x="76" y="122" font-size="10" fill="#6b7280">region #0 · block ^bb0(%arg0: memref&lt;8xf32&gt;)　←　函数参数 = 首块的块参数</text>
  <rect x="82" y="130" width="250" height="24" rx="4" fill="#f0f8f2" stroke="#3f9b5c"/>
  <text x="92" y="146" font-size="10" fill="#2f7a45">op: arith.constant ×4</text>
  <rect x="82" y="158" width="534" height="58" rx="6" fill="#fdf1ec" stroke="#e06a3b"/>
  <text x="92" y="174" font-size="11" font-weight="bold" fill="#e06a3b">op: scf.for</text>
  <rect x="96" y="180" width="506" height="30" rx="4" fill="#fff" stroke="#e06a3b" stroke-dasharray="3 3"/>
  <text x="106" y="193" font-size="10" fill="#6b7280">region #0 · block ^bb0(%i: index, %acc: f32)　←　归纳变量与累加器都是块参数</text>
  <text x="106" y="205" font-size="10" fill="#24292f">op: memref.load → arith.addf → scf.yield（terminator）</text>
  <rect x="82" y="220" width="250" height="24" rx="4" fill="#f0f8f2" stroke="#3f9b5c"/>
  <text x="92" y="236" font-size="10" fill="#2f7a45">op: func.return（terminator）</text>
  <text x="352" y="236" font-size="10" fill="#6b7280">每个 block 的最后一个 op 必须是 terminator</text>
</svg>
<div class="caption">图 2-2　上一段 IR 的树形结构。`scf.for` 不是"控制流指令"，它是一个**带 region 的普通 op**——这就是为什么 MLIR 能在保留循环结构的前提下做变换，而不必先拆成分支再靠分析认回来</div>
</div>

### 为什么不需要 phi：块参数

LLVM IR 里合并控制流要用 `phi`：

<div class="compare">
<div class="known">
<div class="compare-head">LLVM IR：phi 指令</div>

```llvm-norun
define i32 @select_max(i32 %a, i32 %b) {
entry:
  %c = icmp sgt i32 %a, %b
  br i1 %c, label %bb1, label %bb2
bb1:
  br label %merge
bb2:
  br label %merge
merge:
  %r = phi i32 [ %a, %bb1 ], [ %b, %bb2 ]
  ret i32 %r
}
```

</div>
<div class="cpp-side">
<div class="compare-head">MLIR：块参数 + 分支传参</div>

```mlir
func.func @select_max(%a: i32, %b: i32) -> i32 {
  %c = arith.cmpi sgt, %a, %b : i32
  cf.cond_br %c, ^bb1, ^bb2
^bb1:
  cf.br ^merge(%a : i32)
^bb2:
  cf.br ^merge(%b : i32)
^merge(%r: i32):
  return %r : i32
}
```

</div>
</div>

两边语义完全一样，但 MLIR 这边**没有 phi 这种"必须在块开头、与前驱一一对应"的特殊指令**。值是由分支像函数调用一样"传"进目标块的，块参数就是形参。

这不只是审美问题。`phi` 在真实编译器里制造了一大堆特例：删一个前驱块要同步改所有 phi；phi 的"并行赋值"语义容易写错；函数参数又是另一套机制。改成块参数后，这些特例全没了——函数参数就是首块的块参数，删前驱就是删一条分支的实参。官方 `mlir/docs/Rationale/Rationale.md` 里 "block arguments vs phi nodes" 一节专门讲这个权衡，值得一读。

跑一下上面那段 MLIR，注意 `mlir-opt` 打印回来时把 `^merge` 规范化成了 `^bb3`，并自动标注了前驱：

```
  ^bb3(%1: i32):  // 2 preds: ^bb1, ^bb2
    return %1 : i32
```

### 两种 region：SSACFG 与 Graph

`Region` 分两类（`mlir/include/mlir/IR/RegionKindInterface.h` 的 `enum class RegionKind`）：

- **`SSACFG`**：普通的控制流语义，要求**支配关系成立**（定义必须支配使用）。函数体、循环体都是这种。
- **`Graph`**：没有控制流概念，op 之间只有数据依赖，**不要求支配**——所以可以互相引用、可以有环。硬件描述、数据流图这类方言会用它。

绝大多数时候你打交道的都是 SSACFG。但知道 `Graph` 存在很重要：它解释了为什么 MLIR 的很多工具（比如支配关系分析）要先问一句"这个 region 是什么类型"。

### 一个跨 region 的关键规则：IsolatedFromAbove

上面第 4 点说"内层 region 可以引用外层的值"。但有个例外，而且极其重要：带 **`IsolatedFromAbove`** 这个 trait 的 op，它的 region **不允许**引用外部的值——所有输入必须显式传进来。

`func.func` 就带这个 trait（函数体不能随便引用别的函数里的值，这符合直觉），`builtin.module` 也带。这条规则有两个大后果：

- **pass 可以并发跑**。第 13 章会讲：正因为每个 `func.func` 与外界隔离，pass 管理器才能安全地把多个函数丢给多个线程同时处理。
- **outlining 才可能**。第 25 章把循环体抽成 GPU kernel（一个 `gpu.func`），前提就是先把它引用的外部值都变成显式参数。

源码里它叫 `OpTrait::IsIsolatedFromAbove`（`mlir/include/mlir/IR/OpDefinition.h`），ODS 里写作 `IsolatedFromAbove`（第 8 章你会亲手给 op 加 trait）。

## 属性：`<{...}>` 与 `{...}` 不是一回事

回头看通用格式的输出，你会注意到两种括号：

```mlir-norun
// 尖括号：固有属性（存在 properties 里）
%6 = "arith.addf"(%arg2, %5) <{fastmath = #arith.fastmath<none>}> : (f32, f32) -> f32

// 花括号：可丢弃属性（存在通用字典里）
%7 = "arith.addf"(%arg2, %5) {my_pass.visited = true} : (f32, f32) -> f32
```

区别是**语义责任归谁**：

- **固有属性（inherent attribute）**：是 op 定义的一部分，op 的语义依赖它。`arith.constant` 的 `value`、`func.func` 的 `sym_name` 和 `function_type` 都是。它们**必须**存在且合法，verifier 会查（第 10 章）。采用了 properties 存储的方言，这些属性存在 op 自己的内联存储里，打印成 `<{...}>`。
- **可丢弃属性（discardable attribute）**：语义定义在 op 之外，谁都能挂，**丢了不影响 IR 合法性**。典型用途是 pass 之间传信息、打标记。

<div class="warn">
<strong>别把重要语义放在可丢弃属性里</strong>
<p>这正是第 1 章批评 LLVM metadata 的那个问题——可丢弃的东西不能承载"必须成立"的语义，因为任何一次变换都可以合法地把它抹掉。如果某个信息丢了会让程序算错，它就该是固有属性、或者干脆体现在类型里（第 4 章）、或者用一个专门的 op 表达。</p>
</div>

## 动手：把一段 IR 的骨架读出来

下面这段 IR 有点绕：一个 `scf.if` 嵌在 `scf.for` 里，还引用了外层的值。先**不要**运行，自己回答四个问题，再跑一遍核对：

```mlir
// RUN: mlir-opt %s --mlir-print-op-generic
func.func @clamp_sum(%buf: memref<8xf32>, %limit: f32) -> f32 {
  %c0 = arith.constant 0 : index
  %c1 = arith.constant 1 : index
  %c8 = arith.constant 8 : index
  %zero = arith.constant 0.0 : f32
  %total = scf.for %i = %c0 to %c8 step %c1 iter_args(%acc = %zero) -> (f32) {
    %v = memref.load %buf[%i] : memref<8xf32>
    %too_big = arith.cmpf ogt, %v, %limit : f32
    %clamped = scf.if %too_big -> (f32) {
      scf.yield %limit : f32
    } else {
      scf.yield %v : f32
    }
    %next = arith.addf %acc, %clamped : f32
    scf.yield %next : f32
  }
  return %total : f32
}
```

1. 一共有几个 region？分别属于哪个 op？（提示：数 op，不是数缩进）
2. `%limit` 被用在了第几层 region 里？它跨了几层 region 边界？为什么这是合法的？
3. 这段 IR 里哪些 op 是 terminator？一共几个？
4. `scf.if` 有两个 region（then 和 else），它们各有几个 block？块参数有几个？

跑 `mlir-opt clamp_sum.mlir --mlir-print-op-generic` 对答案。数清楚了，你对 MLIR 的结构就有手感了。

**再改一改。** 这三处改动的报错都有点出人意料，正好把"谁在检查什么"这件事分清楚：

1. 把 `scf.yield %limit : f32` 改成 `scf.yield %too_big : f32`。报错是 `use of value '%too_big' expects different type than prior uses: 'f32' vs 'i1'`——注意措辞：这是**解析器（parser）**抓到的，不是 verifier。因为 `%too_big` 在前面已经有类型了，文本格式里同一个名字不能有两种类型。**verifier 还没来得及上场**。
2. 删掉 else 里的 `scf.yield`，只留一个空的 else。你可能以为会看到"block 缺 terminator"，但实际报错是 `'scf.if' op along control flow edge from Operation scf.yield to Operation scf.if: region branch point has 0 operands, but region successor needs 1 inputs`。为什么会冒出一个 `scf.yield`？因为 `scf.if` 声明了**隐式 terminator**——你不写，它替你插一个空的 `scf.yield`，于是错误变成了"yield 了 0 个值，但 `scf.if` 要 1 个结果"。这条检查来自 `RegionBranchOpInterface`（第 5 章讲接口，第 10 章讲验证顺序）。
3. 想看**真正的支配关系报错**，`scf.if` 这个例子办不到——你甚至没法在外面写出那个名字（报错只会是 `use of undeclared SSA value name`，因为文本格式里 SSA 名字是按 region 作用域的）。换成显式控制流才能触发：

```mlir-norun
func.func @dom2(%c: i1) -> i32 {
  cf.cond_br %c, ^bb1, ^bb2
^bb1:
  %x = arith.constant 1 : i32
  cf.br ^bb3
^bb2:
  cf.br ^bb3
^bb3:
  return %x : i32        // %x 只在 ^bb1 里定义，不支配 ^bb3
}
```

跑它会得到：`error: operand #0 does not dominate this use`，还附带一条 `note: operand defined here (op in the same region)` 指向定义处。**这才是 SSACFG region 那条"定义必须支配使用"规则的执行者。** 顺便体会一下 MLIR 诊断的质量：错误 + note 指向两处，都带位置——这是第 10 章要教你为自己的 op 做到的。

---

**这一章的要点**

- MLIR 把 LLVM IR 里所有"特殊的东西"化归成了 op：**模块是 op、函数是 op、常量是 op**。名字是运行时字符串，这才有了可扩展性。
- `Operation` 用 `TrailingObjects` 一次分配：操作数存储、属性存储、后继块、region、结果的 use 边全在同一块内存里；op 同时是侵入式链表节点，插删 O(1)。
- `Value` 只有两种来源：**OpResult 与 BlockArgument**。函数参数不是特例，它是首块的块参数。
- def-use 的边 `OpOperand` **是对象**，值维护一条 use 链表——于是有了 `replaceAllUsesWith`，你已经亲手实现过一遍：**所有 pattern 重写最后都落到它身上**。
- **Region 让 IR 成为一棵树**：op → region → block → op。循环、条件都是"带 region 的普通 op"，结构不用先拆掉再认回来。
- 没有 phi：**块参数 + 分支传参**，消掉了一堆特例。
- `IsolatedFromAbove` 是"region 不能引用外部值"的声明，它是 **pass 并发**与 **kernel outlining** 的前提。
- 属性分**固有**（`<{...}>`，语义必需、verifier 查）与**可丢弃**（`{...}`，随时可丢）。别把关键语义放进可丢弃属性——那就是 LLVM metadata 的老问题。

下一章我们把"怎么读写和摆弄这些 IR"练成肌肉记忆：通用格式与自定义格式的关系（谁是真相）、`Location` 为什么不可选、以及 `mlir-opt` 那几个你以后每天都会敲的选项——`--pass-pipeline`、`--mlir-print-ir-after-change`、`--allow-unregistered-dialect`。
