# 第 7 章 内存层级：global、shared、register 与合并访问

## 这一章想让你带走什么

<div class="goal-box">
<p>第 3 章说过：多数 kernel 的瓶颈不是算得慢，而是<strong>数据喂不上来</strong>。这一章把 GPU 的内存层级摊开讲清楚：每个线程私有的<strong>寄存器（register）</strong>最快、每个 block 共享的<strong>shared memory</strong> 是你亲手管理的片上暂存区、片外的<strong>全局内存（global memory）</strong>又大又慢。</p>
<p>更重要的是那个能让性能差出几十倍的核心概念——<strong>合并访问（memory coalescing）</strong>：当一个 warp 的 32 条 lane 访问<strong>连续</strong>地址时，硬件把它们合并成一次宽传输；访问<strong>跨步/散乱</strong>时，则退化成几十次小传输。读完你会跑一个模拟器数清楚每种访存模式要几次传输，还会读到一个完整可编译的 CUDA 程序，第一次亲手用上 shared memory。</p>
</div>

## 三级内存：越快越小，越大越慢

CPU 也有内存层级（寄存器 → L1 → L2 → L3 → 主存），但那套层级对你是**透明**的：你只管访问变量，硬件自动决定放哪级缓存。GPU 的层级不但更陡，还有一部分**要你亲手管理**。从快到慢，三个你必须区分的层级：

- **寄存器（register）**：每个线程**私有**，就在计算核心旁边，**最快**（0 延迟量级）。你 kernel 里的局部变量（`int i`、`float sum`）默认就住在寄存器里。每个线程能用的寄存器有限——用超了会"溢出"到慢的本地内存。
- **shared memory（共享内存）**：每个 **block 内共享**，物理上就是第 3 章说的那块片上 L1 高速内存，比全局内存快一两个数量级。关键在于：**它由你的程序显式管理**——你决定往里放什么、什么时候放。它是这一章的主角。
- **全局内存（global memory）**：就是你买卡看的"24GB 显存"，片外 DRAM，**又大又慢**（一次访问几百个时钟周期）。所有线程都能读写，是 host 和 device 之间搬数据的落脚点。

此外还有两个你会偶尔碰到的：**常量内存（constant memory）**——小、只读、带专门缓存，适合放所有线程都读同一个值的参数；**L2 缓存**——全片共享，介于 SM 和全局内存之间，自动工作、不用你管。

<div class="concept">
<svg viewBox="0 0 560 320" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
  <text x="280" y="20" text-anchor="middle" font-size="14" font-weight="bold" fill="#2b6cb0">GPU 内存金字塔：越往上越快越小</text>
  <!-- register -->
  <polygon points="230,36 330,36 310,74 250,74" fill="#f0fdf4" stroke="#3f9b5c"/>
  <text x="280" y="60" text-anchor="middle" font-size="12" fill="#2f7a45" font-weight="bold">寄存器</text>
  <text x="345" y="52" font-size="10" fill="#2f7a45">最快 · 每线程私有 · ~几十 KB/SM</text>
  <text x="345" y="66" font-size="10" fill="#6b7280">作用域：单个线程</text>
  <!-- shared -->
  <polygon points="250,78 310,78 290,116 270,116" fill="#fff4ec" stroke="#e06a3b"/>
  <polygon points="200,78 250,78 270,116 180,116" fill="#fff4ec" stroke="#e06a3b"/>
  <polygon points="310,78 360,78 380,116 290,116" fill="#fff4ec" stroke="#e06a3b"/>
  <text x="280" y="102" text-anchor="middle" font-size="12" fill="#a53f1c" font-weight="bold">shared memory / L1</text>
  <text x="395" y="94" font-size="10" fill="#a53f1c">很快 · 片上 · 你亲手管 · ~百 KB/SM</text>
  <text x="395" y="108" font-size="10" fill="#6b7280">作用域：一个 block</text>
  <!-- L2 -->
  <polygon points="160,120 400,120 420,158 140,158" fill="#e5edf5" stroke="#2b6cb0"/>
  <text x="280" y="144" text-anchor="middle" font-size="12" fill="#2b6cb0" font-weight="bold">L2 缓存（全片共享，自动）</text>
  <text x="435" y="142" font-size="10" fill="#6b7280">作用域：整颗 GPU</text>
  <!-- global -->
  <polygon points="120,162 440,162 470,300 90,300" fill="#3c3c3c"/>
  <text x="280" y="215" text-anchor="middle" font-size="14" fill="#9fe3b0" font-weight="bold">全局内存 / 显存（DRAM）</text>
  <text x="280" y="238" text-anchor="middle" font-size="11" fill="#cbd5e1">最慢（~几百周期）· 最大（GB 级）· 所有线程可见</text>
  <text x="280" y="262" text-anchor="middle" font-size="11" fill="#cbd5e1">host↔device 拷贝的落脚点</text>
  <!-- arrows -->
  <text x="70" y="60" font-size="11" fill="#6b7280">快</text>
  <text x="70" y="290" font-size="11" fill="#6b7280">慢</text>
  <line x1="75" y1="70" x2="75" y2="280" stroke="#6b7280" stroke-width="1.5" marker-end="url(#dn)"/>
  <defs><marker id="dn" markerWidth="9" markerHeight="9" refX="4" refY="7" orient="auto"><path d="M0,0 L8,0 L4,7 Z" fill="#6b7280"/></marker></defs>
</svg>
<div class="caption">图 7-1　GPU 内存金字塔。寄存器/shared 在 SM 片上、极快极小；全局内存在片外、又大又慢。速度和容量差着一两个数量级，作用域也各不相同</div>
</div>

## shared memory：你自己管的缓存

CPU 的缓存对你是自动、透明的：你访问一个数组，硬件默默把它周围的数据拉进 L1，下次命中就快。你从不写"把这块数据放进 L1"的代码——你**没法**这么写。

GPU 的 shared memory 恰恰相反：它是一块**由你显式管理**的片上高速内存。你在 kernel 里用 `__shared__` 声明一块，然后**亲手**把全局内存里的数据搬进去、用完再说。硬件不会替你决定放什么。

<div class="compare">
<div class="known">
<div class="compare-head">CPU 缓存：自动、透明</div>

```cpp-norun
// 你只管访问，硬件自动缓存
for (int i = 0; i < n; ++i)
    sum += a[i];   // a[] 被自动拉进 L1
// 你无法、也不需要控制"放什么进缓存"
// 命中与否由硬件的替换策略决定
```

</div>
<div class="cpp-side">
<div class="compare-head">GPU shared memory：显式、手动</div>

```cuda-norun
// 你亲手声明、亲手搬、亲手同步
__shared__ float tile[256];
tile[threadIdx.x] = a[globalIdx]; // 你决定放什么
__syncthreads();                  // 等全组搬完
// 之后大家反复读 tile[]，不再碰慢的全局内存
```

</div>
</div>

<div class="keypoint">
<strong>一句话记住 shared memory</strong>
它是"<strong>你自己管的缓存</strong>"。CPU 的缓存是硬件替你猜哪些数据该留在近处；GPU 把这个决定权交给了你。它的黄金用法是<strong>数据复用</strong>：如果一块数据要被 block 里很多线程反复读，就先花一次慢的全局内存访问把它搬进 shared memory，之后所有复用都走快的片上访问。第 11 章的 tiling 矩阵乘就是这招的教科书级应用。
</div>

## 合并访问：这一章最值钱的概念

现在讲那个能让性能差出几十倍、却常被新手忽略的东西。回忆第 6 章：全局内存访问不是一个线程一个线程发出的，而是**以 warp 为单位**——一个 warp 的 32 条 lane 在同一条 load 指令上，同时各自要一个地址的数据。

硬件访问全局内存有个物理特性：它一次只能按**固定大小的对齐块**搬运（典型是 128 字节一段，即 32 个 `float`）。于是关键问题变成：**这个 warp 的 32 个地址，落在几个 128 字节的段里？**

- 如果 32 条 lane 要的是**连续**的 32 个 `float`（lane *i* 读 `a[base+i]`），它们正好落在**同一个** 128 字节段里——硬件一次传输就全部搞定。这叫**合并访问（memory coalescing）**。
- 如果 32 条 lane 的地址**跨步或散乱**（比如 lane *i* 读 `a[i*32]`），32 个地址落进 32 个不同的段——硬件只能发 **32 次**传输，其中每次搬来的 128 字节里只用了 4 字节，**97% 的带宽被浪费**。

<div class="concept">
<svg viewBox="0 0 560 300" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
  <text x="280" y="18" text-anchor="middle" font-size="14" font-weight="bold" fill="#2b6cb0">合并 vs 跨步：同样 32 次读取，传输次数天差地别</text>
  <!-- coalesced -->
  <text x="140" y="44" text-anchor="middle" font-size="12" fill="#3f9b5c" font-weight="bold">合并（stride=1）</text>
  <text x="140" y="60" text-anchor="middle" font-size="10" fill="#6b7280">32 条 lane 读连续 32 个 float</text>
  <g font-size="9">
    <text x="20" y="82" fill="#2b6cb0">lane 0..31</text>
    <g fill="#eef6ff" stroke="#2b6cb0" stroke-width="0.6">
      <rect x="20" y="88" width="7" height="16"/><rect x="27" y="88" width="7" height="16"/><rect x="34" y="88" width="7" height="16"/><rect x="41" y="88" width="7" height="16"/><rect x="48" y="88" width="7" height="16"/><rect x="55" y="88" width="7" height="16"/><rect x="62" y="88" width="7" height="16"/><rect x="69" y="88" width="7" height="16"/><rect x="76" y="88" width="7" height="16"/><rect x="83" y="88" width="7" height="16"/><rect x="90" y="88" width="7" height="16"/><rect x="97" y="88" width="7" height="16"/><rect x="104" y="88" width="7" height="16"/><rect x="111" y="88" width="7" height="16"/><rect x="118" y="88" width="7" height="16"/><rect x="125" y="88" width="7" height="16"/><rect x="132" y="88" width="7" height="16"/><rect x="139" y="88" width="7" height="16"/><rect x="146" y="88" width="7" height="16"/><rect x="153" y="88" width="7" height="16"/><rect x="160" y="88" width="7" height="16"/><rect x="167" y="88" width="7" height="16"/><rect x="174" y="88" width="7" height="16"/><rect x="181" y="88" width="7" height="16"/><rect x="188" y="88" width="7" height="16"/><rect x="195" y="88" width="7" height="16"/><rect x="202" y="88" width="7" height="16"/><rect x="209" y="88" width="7" height="16"/><rect x="216" y="88" width="7" height="16"/><rect x="223" y="88" width="7" height="16"/><rect x="230" y="88" width="7" height="16"/><rect x="237" y="88" width="7" height="16"/>
    </g>
    <line x1="130" y1="112" x2="130" y2="130" stroke="#3f9b5c" stroke-width="1"/>
    <rect x="20" y="132" width="224" height="20" rx="3" fill="#f0fdf4" stroke="#3f9b5c"/>
    <text x="132" y="146" text-anchor="middle" font-size="10" fill="#2f7a45">1 段 128 字节 → 1 次传输 ✓</text>
  </g>
  <!-- strided -->
  <text x="420" y="44" text-anchor="middle" font-size="12" fill="#e06a3b" font-weight="bold">跨步（stride=32）</text>
  <text x="420" y="60" text-anchor="middle" font-size="10" fill="#6b7280">32 条 lane 读散在各处的 float</text>
  <g font-size="9">
    <text x="300" y="82" fill="#2b6cb0">lane 0</text>
    <text x="360" y="82" fill="#2b6cb0">lane 1</text>
    <text x="480" y="82" fill="#2b6cb0">…lane 31</text>
    <g fill="#fbe6dc" stroke="#e06a3b" stroke-width="0.6">
      <rect x="300" y="88" width="7" height="16"/>
      <rect x="360" y="88" width="7" height="16"/>
      <rect x="420" y="88" width="7" height="16"/>
      <rect x="480" y="88" width="7" height="16"/>
      <rect x="520" y="88" width="7" height="16"/>
    </g>
    <line x1="303" y1="112" x2="303" y2="128" stroke="#e06a3b" stroke-width="1"/>
    <line x1="363" y1="112" x2="363" y2="128" stroke="#e06a3b" stroke-width="1"/>
    <line x1="423" y1="112" x2="423" y2="128" stroke="#e06a3b" stroke-width="1"/>
    <line x1="483" y1="112" x2="483" y2="128" stroke="#e06a3b" stroke-width="1"/>
    <g fill="#fff4ec" stroke="#e06a3b">
      <rect x="292" y="130" width="22" height="18" rx="2"/><rect x="352" y="130" width="22" height="18" rx="2"/><rect x="412" y="130" width="22" height="18" rx="2"/><rect x="472" y="130" width="22" height="18" rx="2"/><rect x="510" y="130" width="22" height="18" rx="2"/>
    </g>
    <text x="420" y="168" text-anchor="middle" font-size="10" fill="#a53f1c">32 个段 → 32 次传输 ✗（每次只用 1/32）</text>
  </g>
  <line x1="280" y1="70" x2="280" y2="180" stroke="#d1d5db" stroke-dasharray="4 3"/>
  <text x="280" y="215" text-anchor="middle" font-size="11" fill="#6b7280">同样是 32 个 float 的读取，合并只需 1 次传输，跨步要 32 次</text>
  <text x="280" y="236" text-anchor="middle" font-size="11" fill="#e06a3b" font-weight="bold">访存的"顺序"比"次数"更影响速度——这是 GPU 最反 CPU 直觉的一点</text>
</svg>
<div class="caption">图 7-2　合并访问 vs 跨步访问。warp 的 32 个地址落在同一个 128 字节段里就是 1 次传输；散落在 32 个段里就是 32 次传输，带宽利用率天差地别</div>
</div>

<div class="warn">
<strong>合并的判据是"warp 内相邻 lane 访问相邻地址"</strong>
这解释了一个新手常犯的错：处理二维数据时，让相邻线程沿<strong>列</strong>方向走（<code>a[tid * width + col]</code>），相邻 lane 的地址就差了一整行，彻底不合并；改成沿<strong>行</strong>走（<code>a[row * width + tid]</code>），相邻 lane 访问相邻地址，立刻合并。同样的算法、同样的数据量，仅仅换了循环下标的角色，就可能快几十倍。第 17 章会把访存优化（合并、向量化、bank conflict）讲成一套系统方法。
</div>

## bank conflict：shared memory 也有它的"访存模式"（先一瞥）

shared memory 快，但它内部被分成 32 个 **bank（存储体）**，一个 warp 同时访问时，如果多条 lane 撞进**同一个 bank 的不同地址**，就会 **bank conflict（存储体冲突）**——硬件被迫串行处理，快内存也变慢。规律和合并类似：warp 内 lane 访问 shared 数组的下标错开成不同 bank 才最快。这里先埋个点，第 17 章详解；现在只需知道"shared memory 也讲究访问模式"。

## 动手：数一数每种访存模式要几次传输

下面这个模拟器**能在浏览器里直接跑**——点 **▶ 运行**。它把上面那张图变成可计算的：给定一个 warp 的访问步长（stride），算出这 32 条 lane 的地址落进几个 128 字节段，也就是需要几次内存传输。你会看到 stride=1 只要 1 次，stride=32 要 32 次。

```python
# 模拟：一个 warp（32 条 lane）按某步长访问 float，需要几次 128 字节传输
WARP = 32
BYTES_PER_FLOAT = 4
SEGMENT = 128                       # 硬件一次搬运的对齐块大小（字节）

def transactions(stride, base_elem=0):
    """每条 lane 读 a[base + lane*stride]，返回覆盖到的 128 字节段数。"""
    segments = set()
    for lane in range(WARP):
        byte_addr = (base_elem + lane * stride) * BYTES_PER_FLOAT
        segments.add(byte_addr // SEGMENT)     # 这个地址属于哪个段
    return len(segments)

print(f"一个 warp = {WARP} 条 lane，每次搬运一个对齐的 {SEGMENT} 字节段\n")
print("步长 stride | 需要的传输次数 | 带宽利用率")
print("-" * 46)
for stride in [1, 2, 4, 8, 16, 32]:
    n = transactions(stride)
    used = WARP * BYTES_PER_FLOAT          # 真正需要的字节
    moved = n * SEGMENT                    # 实际搬运的字节
    print(f"{stride:>9}   | {n:>10} 次   | {used/moved*100:5.1f}%")

print("\n结论：")
print("  stride=1（合并）：32 个 float 正好铺满 1 个段 → 1 次传输，100% 有效。")
print("  stride 越大越散：段数越多、每段浪费越狠。")
print("  stride=32：32 个地址落进 32 个段 → 32 次传输，只用了 1/32 的带宽。")
```

运行结果：

```
一个 warp = 32 条 lane，每次搬运一个对齐的 128 字节段

步长 stride | 需要的传输次数 | 带宽利用率
----------------------------------------------
        1   |          1 次   | 100.0%
        2   |          2 次   |  50.0%
        4   |          4 次   |  25.0%
        8   |          8 次   |  12.5%
       16   |         16 次   |   6.2%
       32   |         32 次   |   3.1%
```

一张表把"访存模式决定性能"讲透了：从合并（stride=1，一次传输、满带宽）到最散（stride=32，32 次传输、只用 3% 带宽），**算的活一模一样，仅仅因为地址的排布，带宽利用率就差了 32 倍**。请动手改：把 `transactions(stride)` 调用改成 `transactions(1, base_elem=1)`（连续访问，但整体偏移一个 float）——你可能以为还是 1 次，但因为地址不再对齐到 128 字节边界，它会变成 2 次。这就是为什么真实优化里除了"连续"还讲究"对齐"。

## 动手：第一个用上 shared memory 的完整 kernel

下面是一个**完整可编译**的 CUDA 程序：每个 block 把自己那段数据搬进 shared memory，`__syncthreads()` 等全组搬完，然后每个线程去读**自己 block 内的邻居**——做一个"在 block 内把数据首尾颠倒"的操作。它同时演示了 shared memory 的两大要素：**显式搬运** 和 **同步等待**。点 **🔗 在 Compiler Explorer 打开** 可以看它编译出的 PTX。

```cuda
// 用 shared memory 在每个 block 内部把数据首尾颠倒
#include <cstdio>
#include <cuda_runtime.h>

#define BLOCK 256

__global__ void reverseInBlock(float* out, const float* in, int n) {
    __shared__ float tile[BLOCK];                 // 每个 block 一块片上暂存区

    int gid = blockIdx.x * blockDim.x + threadIdx.x;
    int t   = threadIdx.x;

    // 1) 合并访问：相邻线程读相邻地址，把本 block 的数据搬进 shared memory
    if (gid < n) tile[t] = in[gid];
    __syncthreads();                              // 等本 block 所有线程都搬完

    // 2) 每个线程读"block 内的镜像邻居"——数据已在快内存里，随便挑
    int mirror = blockDim.x - 1 - t;
    if (gid < n) out[gid] = tile[mirror];
}

int main() {
    const int N = 1024;
    size_t bytes = N * sizeof(float);

    float *hIn = (float*)malloc(bytes), *hOut = (float*)malloc(bytes);
    for (int i = 0; i < N; ++i) hIn[i] = (float)i;    // 0,1,2,...

    float *dIn, *dOut;
    cudaMalloc(&dIn, bytes);
    cudaMalloc(&dOut, bytes);
    cudaMemcpy(dIn, hIn, bytes, cudaMemcpyHostToDevice);

    int blocks = (N + BLOCK - 1) / BLOCK;
    reverseInBlock<<<blocks, BLOCK>>>(dOut, dIn, N);
    cudaDeviceSynchronize();

    cudaMemcpy(hOut, dOut, bytes, cudaMemcpyDeviceToHost);

    // 第 0 个 block 内应从 255 反到 0；打印头尾验证
    printf("out[0]   = %.0f  (期望 255)\n", hOut[0]);
    printf("out[255] = %.0f  (期望 0)\n",   hOut[255]);
    printf("out[256] = %.0f  (期望 511，第二个 block 内部反转)\n", hOut[256]);

    cudaFree(dIn); cudaFree(dOut);
    free(hIn); free(hOut);
    return 0;
}
```

编译运行（本地有 NVIDIA GPU 时）：

```bash
nvcc reverse.cu -o reverse && ./reverse
```

运行结果：

```
out[0]   = 255  (期望 255)
out[255] = 0  (期望 0)
out[256] = 511  (期望 511，第二个 block 内部反转)
```

注意这个 kernel 的结构，它是后面几乎所有"用 shared memory 提速"的 kernel 的骨架：**搬进来 → `__syncthreads()` → 反复读**。这里为什么必须 `__syncthreads()`？因为线程 0 想读 `tile[255]`，而 `tile[255]` 是线程 255 搬进去的——如果不等线程 255 写完就读，线程 0 会读到垃圾。这就是 GPU 上代替锁的协作方式：**barrier（栅栏同步）**。请动手改：把 `mirror` 那行改成 `int mirror = (t + 1) % blockDim.x;`（读右边邻居，循环回卷），重新编译运行，看输出怎么变——你会看到每个 block 内的数据整体左移了一位。

## 小结与下一步

你现在有了 GPU 内存的完整地图：寄存器（每线程、最快）、shared memory（每 block、片上、你亲手管）、全局内存（片外、又大又慢），以及那条铁律——**warp 内相邻 lane 访问相邻地址才能合并成一次传输，否则退化成几十次**。访存的顺序，比访存的次数更决定速度。

上面那个 reverse kernel 里，你不得不写下一行 `__syncthreads()`——没有它，程序就是错的。这个"等所有线程到齐再往下走"的动作，正是 GPU 协作的核心。下一章我们就专门讲**协作与同步**：`__syncthreads()` 这样的 barrier 到底保证了什么、第 6 章埋下的 warp 级 shuffle 怎么用、以及当多个线程要往同一个地址累加时，如何用**原子操作**避免数据竞争。

---

**这一章的要点**

- GPU 内存分三级：**寄存器**（每线程私有、最快）、**shared memory**（每 block 共享、片上、你显式管理）、**全局内存**（片外、又大又慢、所有线程可见）；另有常量内存和自动的 L2。
- shared memory 是"**你自己管的缓存**"：与 CPU 自动透明的缓存不同，你亲手声明、亲手搬数据、亲手同步。黄金用法是**数据复用**（第 11 章 tiling）。
- **合并访问（memory coalescing）** 是本章最值钱的概念：warp 的 32 条 lane 访问**连续对齐**地址时合并成 1 次宽传输；**跨步/散乱**时退化成多达 32 次小传输，带宽利用率暴跌。
- 判据是"**warp 内相邻 lane 访问相邻地址**"；二维数据里沿行还是沿列遍历，可能带来几十倍性能差。
- shared memory 也讲访问模式：多条 lane 撞进同一 **bank** 会 bank conflict（第 17 章详解）。
- 用 shared memory 的骨架是：**搬进来 → `__syncthreads()` → 反复读**；那句同步不是可选的，缺了就是数据竞争——下一章展开。
